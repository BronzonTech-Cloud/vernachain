import React from 'react';
import { Box, Heading } from '@chakra-ui/react';

const Validators: React.FC = () => {
  return (
    <Box>
      <Heading mb={6}>Active Validators</Heading>
    </Box>
  );
};

export default Validators; 